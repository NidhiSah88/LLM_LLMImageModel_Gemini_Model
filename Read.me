conda create -p ./venv python=3.10 -y

conda activate ./venv

# To activate this environment, use
#
#     $ conda activate /Users/nidhikumari/Desktop/KRISHNAIKGENAIPROJECTS/Project_Langchain/GeminiLLMApp/venv
#
# To deactivate an active environment, use
#
#     $ conda deactivate


verify environments exist: 

conda env list
python --version


To run : streamlit run app.py 

pip show streamlit
pip show starlette





Clean setup from scratch

If you're just starting the project, do:

conda deactivate

conda remove -p ./venv --all -y

conda create -p ./venv python=3.10 -y

conda activate ./venv

pip install -r requirements.txt

python -m streamlit run app.py




Check which models your API key can access

Create a small file:

import google.generativeai as genai
import os
from dotenv import load_dotenv

load_dotenv()

genai.configure(api_key=os.getenv("GOOGLE_API_KEY"))

for model in genai.list_models():
    if "generateContent" in model.supported_generation_methods:
        print(model.name)

Run:

python test_models.py

This will show the exact model names available to your API key.


conda activate ./venv            
python -m streamlit run vision.py


git pull --rebase origin main







